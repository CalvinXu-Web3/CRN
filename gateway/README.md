# Share Gateway placeholder

Implement this service in a server or edge runtime when dynamic per-case Open Graph metadata is required. It must return only source-reviewed case titles, descriptions, images and URLs; the static H5 intentionally remains API-free in Core mode.
